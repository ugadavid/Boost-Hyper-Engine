/**
 * # 📦 Models
 *
 * # 📦 Dossier models
 * 
 * Ce dossier contient les **structures de données** représentant des entités du moteur (joueurs, mobs, inventaire...).
 * 
 * ## Philosophie
 * 
 * - Chaque fichier définit **un seul modèle logique** exporté
 * - Tous les modèles sont **typiquement des `interface` ou `class`**
 * - Pas de logique métier ici — seulement des **représentations de données**
 * 
 * ## Bonnes pratiques
 * 
 * - Utiliser des noms explicites : `Player`, `Mob`, `InventoryItem`
 * - Ajouter des commentaires JSDoc à chaque propriété
 * - Favoriser les `readonly` si l’objet ne doit pas être modifié
 * 
 */
export const __modelsDoc__ = true;